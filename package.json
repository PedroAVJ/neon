{
  "name": "@pedroavj/neon-plugin",
  "version": "0.2.4",
  "private": true,
  "description": "Dual-client skill for deliberate, short-lived Neon branches.",
  "license": "MIT",
  "scripts": {
    "check": "node --test ./tests/*.test.cjs",
    "test": "npm run check"
  }
}
